{
  "vault_path": "/path/to/your/obsidian/vault",
  "daily_notes_folder": "Daily Notes",
  "date_format": "YYYY-MM-DD",
  "daily_brief": {
    "output_dir": "output/daily",
    "sources": [
      "ai_radar"
    ]
  },
  "lookback_hours": 24,
  "deep_dive_default_days": 7,
  "scripts_path": "/path/to/morning-brief-agent/scripts",
  "open_work": {
    "pr": {
      "stale_days": 3,
      "very_stale_days": 7,
      "note": "Days since a PR's last activity (updated_at) before it is tagged 🟡 stale / 🔴 very-stale in the Open Work brief section (spec 009-01). Defaults used if this block is absent."
    }
  },
  "tools": {
    "slack": {
      "enabled": true,
      "gather_method": "plugin",
      "gather_fallback": "script",
      "draft_method": "slack_native_draft",
      "draft_enabled": true,
      "url": "https://app.slack.com",
      "note": "Gather is plugin-first (Slack plugin) with scripts/fetch-slack.js as the bounded fallback (ADR-0004/ADR-0005); these gather_* fields are advisory — morning-slack/SKILL.md is the source of truth. Drafts staged via the Slack plugin's slack_send_message_draft (ADR-0005), gated separately by config/slack.json's own draft_enabled field. No draft fallback if the plugin is unavailable — see docs/architecture.md's 'Slack: Plugin-First With Bounded Fallbacks'."
    },
    "outlook": {
      "enabled": true,
      "gather_method": "connector",
      "gather_fallback": "browser",
      "draft_method": "browser",
      "draft_enabled": true,
      "auto_archive": true,
      "auto_delete": false,
      "url": "https://outlook.office.com",
      "note": "Phase 3 — blocked on Azure App permissions. Browser compose auto-saves to Drafts."
    },
    "teams": {
      "enabled": true,
      "gather_method": "connector",
      "gather_fallback": "browser",
      "draft_method": "none",
      "draft_enabled": false,
      "note": "Teams is scanned via the Outlook skill. Draft staging deferred."
    },
    "jira": {
      "enabled": true,
      "gather_method": "script",
      "draft_method": "local_md",
      "draft_enabled": true,
      "url": "https://jira.yourcompany.com",
      "note": "Drafts written to {vault}/drafts/ as MD fragments via stage-local-draft.js."
    },
    "confluence": {
      "enabled": true,
      "gather_method": "script",
      "draft_method": "local_md",
      "draft_enabled": false,
      "url": "https://confluence.yourcompany.com",
      "note": "Read-only by default. Enable draft_enabled to generate page comment drafts as local MD fragments."
    },
    "github_com": {
      "enabled": true,
      "gather_method": "script",
      "gather_fallback": "connector",
      "draft_method": "github_api",
      "draft_enabled": true,
      "url": "https://github.com",
      "note": "PR reviews: pending review via API (stage-github-review.js). Issue comments: local MD fragments."
    },
    "github_corp": {
      "enabled": true,
      "gather_method": "script",
      "draft_method": "github_api",
      "draft_enabled": true,
      "url": "https://github.yourcompany.com",
      "note": "Same as github_com but uses GITHUB_CORP_TOKEN and GITHUB_CORP_BASE_URL."
    },
    "ai_radar": {
      "enabled": true,
      "gather_method": "script",
      "draft_method": "none",
      "draft_enabled": false,
      "note": "Read-only. RSS/GitHub/HTML watch triage. No draft staging."
    }
  },
  "_draft_methods": {
    "slack_native_draft": "Stage a native Slack draft via the plugin's slack_send_message_draft (ADR-0005). No script fallback — retired stage-slack-draft.js in slice 004-03.",
    "local_md": "Write Markdown fragment to {vault}/drafts/ (stage-local-draft.js)",
    "github_api": "PR reviews: pending review via GitHub API (stage-github-review.js). Issues: local MD.",
    "browser": "Claude in Chrome compose box (auto-saves to tool's native drafts)",
    "none": "No draft staging for this tool"
  }
}
